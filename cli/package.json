{
  "name": "dev-cli",
  "version": "0.1.0",
  "private": true,
  "description": "Project CLI for LemarSyn automation tasks.",
  "bin": {
    "dev-cli": "dist/cli.js"
  },
  "scripts": {
    "build": "tsc -p tsconfig.json && chmod +x dist/cli.js",
    "dev": "tsx src/cli.ts",
    "start": "node dist/cli.js",
    "test": "tsx --test test/**/*.test.ts"
  },
  "dependencies": {
    "commander": "^12.1.0",
    "dotenv": "^16.4.7"
  },
  "devDependencies": {
    "@types/node": "^22.13.10",
    "tsx": "^4.20.4",
    "typescript": "^5.7.3"
  },
  "engines": {
    "node": ">=18"
  }
}
